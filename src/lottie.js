lottie.loadAnimation({
    container: document.getElementById('bee'), // Container element
    renderer: 'svg', // Render as SVG
    loop: true, // Loop the animation
    autoplay: true, // Start automatically
    path: '/bee.json' // Path to the Lottie JSON file
});
lottie.loadAnimation({
    container: document.getElementById('sad'), // Container element
    renderer: 'svg', // Render as SVG
    loop: true, // Loop the animation
    autoplay: true, // Start automatically
    path: '/sad.json' // Path to the Lottie JSON file
});
lottie.loadAnimation({
    container: document.getElementById('warn'), // Container element
    renderer: 'svg', // Render as SVG
    loop: true, // Loop the animation
    autoplay: true, // Start automatically
    path: '/warn.json' // Path to the Lottie JSON file
});
lottie.loadAnimation({
    container: document.getElementById('island'), // Container element
    renderer: 'svg', // Render as SVG
    loop: true, // Loop the animation
    autoplay: true, // Start automatically
    path: '/last.json' // Path to the Lottie JSON file
});

lottie.loadAnimation({
    container: document.getElementById('featherwoman'), // Container element
    renderer: 'svg', // Render as SVG
    loop: true, // Loop the animation
    autoplay: true, // Start automatically
    path: '/ROSE.json' // Path to the Lottie JSON file
});
lottie.loadAnimation({
    container: document.getElementById('fight'), // Container element
    renderer: 'svg', // Render as SVG
    loop: true, // Loop the animation
    autoplay: true, // Start automatically
    path: '/fight.json' // Path to the Lottie JSON file
});